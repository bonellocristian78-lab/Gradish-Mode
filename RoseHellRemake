if game.Workspace:FindFirstChild("SeekMovingNewClone") then return end

local Core = loadstring(game:HttpGet(
	"https://raw.githubusercontent.com/bonellocristian78-lab/Gradish-Mode/main/GradishCore"
))()

Core:Spawn({
	Name  = "Rose Hell",
	Asset = "https://raw.githubusercontent.com/bonellocristian78-lab/Gradish-Mode/main/roseHell.rbxm",

	Speed        = 100,
	Delay        = 2,
	Damage       = 125,
	DamageRange  = 30,
	Rebounds     = { 1, 1 },
	ReboundType  = "Ambush",

	-- A little random pace on every spawn so she is never metronomic.
	Variance = 0.05,

	CameraShake = {
		Enabled = true,
		Range   = 100,
		Values  = { 70, 100, 0.1, 1 },
	},

	DeathCause = "Rose hell",
	-- One of Yellow, Blue, Glitch, Static, Rush -- the real list, read
	-- from the game rather than the spawner's two-entry table.
	DeathType = "Blue",

	DeathHints = {
		"You died to...",
		"Rose Hell. It is an easy one",
		"To beat it, hide each rebound",
		"It acts like ambush, Goodluck.",
	},

	---====== MECHANICS ======---

	-- The pink that was already here, except now it fades back out when she
	-- leaves instead of staying on the map for the rest of the run.
	Tell = {
		Color   = Color3.fromRGB(254, 44, 84),
		Falloff = true,
		Reach   = 5,
		FadeIn  = 1,
		FadeOut = 0.8,
	},

	-- One rebound, and the return trip is faster than the approach. You do
	-- not get to time the second pass off the first.
	Escalate = {
		SpeedPerRebound    = 0.18,
		RangePerRebound    = 4,
		MaxSpeedMultiplier = 1.4,
	},

	-- A quarter of the time she turns around and comes back the other way
	-- once the corridor has already gone quiet.
	Encore = {
		Chance   = 0.25,
		Delay    = 4,
		Reversed = true,
	},

	--[[ THE CRUCIFIX BARELY WORKS ON HER.

	     85% of attempts simply fail -- the ritual plays its failure sound and
	     she carries on. The spawner reads Crucifixion.Resist once, when the
	     ritual starts, so the roll happens at spawn rather than when you raise
	     it: she has already decided before you try.

	     4% of the time it comes up corroded instead. The ritual runs sick
	     green rather than the usual blue, and it works. That is the only way
	     she goes down to a crucifix, and it has its own achievement. ]]
	Crucifixion = {
		Enabled      = true,
		Type         = "Guiding",
		Range        = 40,
		Break        = true,
		ResistChance = 0.85,
	},

	Corroded = {
		Chance      = 0.04,
		Color       = Color3.fromRGB(86, 214, 92),
		Achievement = "Corroded",
	},

	-- Any colour you like for the banish ritual, not just the two the spawner
	-- knows. Left unset here so the normal ritual stays the usual blue.
	-- CrucifixColor = Color3.fromRGB(255, 80, 120),

	-- Achievement wording lives in AchievementConfig, so all four of
	-- this entity's badges are edited in one place with everyone else's.
})
