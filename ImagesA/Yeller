--[[ Yeller -- the violet blades.

     Drawn fresh rather than traced from the reference: those were small,
     dark-background JPEGs with compression haze on every edge. This one has
     real alpha, so it scales without a grey box behind it.

     Source is worked out from the value. This is a URL, so it is fetched once,
     cached on disk and handed to getcustomasset. ]]

return "https://raw.githubusercontent.com/bonellocristian78-lab/Gradish-Mode/main/ImagesA/png/violet.png"
